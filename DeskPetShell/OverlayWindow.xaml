<Window x:Class="DeskPetShell.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="DeskPet (자유 모드)"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <!--
      자유 모드: 작업 영역 전체를 덮는 투명 오버레이.
      일반 WebView2는 AllowsTransparency 창에서 안 그려지므로 CompositionControl 사용.
    -->
    <wv2:WebView2CompositionControl x:Name="Web" />
</Window>
